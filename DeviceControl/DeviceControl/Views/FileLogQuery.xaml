<UserControl
    x:Class="DeviceControl.Views.FileLogQuery"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    d:DesignHeight="450"
    d:DesignWidth="800"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <Grid Background="{DynamicResource RegionBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  工具栏  -->
        <StackPanel Margin="5" Orientation="Horizontal">
            <Button
                MinHeight="40"
                Margin="5"
                Command="{Binding OpenFileCommand}"
                Content="打开日志文件"
                FontSize="16" />
            <CheckBox
                MinHeight="40"
                Margin="15,5,5,5"
                Content="正则表达式"
                FontSize="16"
                IsChecked="{Binding UseRegex}" />
            <Button
                MinHeight="40"
                Margin="5"
                Command="{Binding ClearFilterCommand}"
                Content="清空过滤"
                FontSize="16" />
            <TextBlock
                Margin="20,0,5,0"
                VerticalAlignment="Center"
                FontSize="16"
                Text="状态：" />
            <TextBlock
                VerticalAlignment="Center"
                FontSize="16"
                Text="{Binding StatusText}" />
        </StackPanel>

        <!--  过滤条件行  -->
        <StackPanel
            Grid.Row="1"
            MinHeight="40"
            Margin="5"
            Orientation="Horizontal">
            <TextBlock
                VerticalAlignment="Center"
                FontSize="16"
                Text="过滤：" />
            <TextBox
                Width="300"
                Margin="5,0"
                FontSize="16"
                Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" />
            <ComboBox
                Width="120"
                Margin="5,0"
                FontSize="16"
                ItemsSource="{Binding ColumnOptions}"
                SelectedItem="{Binding SelectedColumn}" />
            <TextBlock
                Margin="15,0,5,0"
                VerticalAlignment="Center"
                FontSize="16"
                Text="高亮：" />
            <TextBox
                Width="200"
                Margin="5,0"
                FontSize="16"
                Text="{Binding HighlightText, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <!--  高性能日志显示区：ListBox 虚拟化 + 水平滚动 + 文本可选择  -->
        <ListBox
            Grid.Row="2"
            Margin="5"
            FontFamily="Consolas"
            FontSize="16"
            ItemsSource="{Binding DisplayLines}"
            ScrollViewer.CanContentScroll="True"
            ScrollViewer.HorizontalScrollBarVisibility="Auto"
            ScrollViewer.VerticalScrollBarVisibility="Visible"
            VirtualizingPanel.IsVirtualizing="True"
            VirtualizingPanel.VirtualizationMode="Recycling">
            <ListBox.Resources>
                <ContextMenu x:Key="LineContextMenu">
                    <MenuItem Click="CopyLine_Click" Header="复制整行" />
                    <MenuItem Click="CopySelectedText_Click" Header="复制选中文本" />
                </ContextMenu>
            </ListBox.Resources>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="ContextMenu" Value="{StaticResource LineContextMenu}" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsHighlighted}" Value="True">
                            <Setter Property="Background" Value="Yellow" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <!--  使用只读 TextBox 实现文本可选择复制  -->
                    <TextBox
                        VerticalAlignment="Center"
                        Background="Transparent"
                        BorderThickness="0"
                        Cursor="IBeam"
                        FontFamily="{Binding RelativeSource={RelativeSource AncestorType=ListBox}, Path=FontFamily}"
                        FontSize="{Binding RelativeSource={RelativeSource AncestorType=ListBox}, Path=FontSize}"
                        IsReadOnly="True"
                        IsTabStop="False"
                        Text="{Binding Text}"
                        TextWrapping="NoWrap">
                        <!--  可选：微调内边距，让选中效果更好看  -->
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TextBox">
                                            <ScrollViewer
                                                x:Name="PART_ContentHost"
                                                HorizontalScrollBarVisibility="Disabled"
                                                VerticalScrollBarVisibility="Disabled" />
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>