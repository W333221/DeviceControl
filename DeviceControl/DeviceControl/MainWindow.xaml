<hc:Window
    x:Class="DeviceControl.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    Title="DeviceControl"
    d:DesignHeight="800"
    d:DesignWidth="1400"
    hc:Empty.ShowEmpty="True"
    prism:ViewModelLocator.AutoWireViewModel="True"
    Background="{DynamicResource SecondaryRegionBrush}"
    Icon="/DeviceControl;component/Resources/Img/logo.ico"
    Loaded="Window_Loaded"
    ResizeMode="CanResizeWithGrip"
    ShowTitle="False"
    Style="{StaticResource WindowWin10}"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Grid>


        <ContentControl prism:RegionManager.RegionName="ControlMain" />

        <hc:NotifyIcon
            x:Name="MyNotifyIcon"
            Icon="/Resources/Img/logo.ico"
            Text="设备控制"
            Visibility="Visible">

            <!--  定义右键菜单  -->
            <hc:NotifyIcon.ContextMenu>
                <ContextMenu>
                    <!--  使用 显示主窗口  -->
                    <MenuItem Command="hc:ControlCommands.PushMainWindow2Top" Header="打开主面板" />
                    <!--  分隔线  -->
                    <Separator />
                    <!--  使用 退出应用  -->
                    <MenuItem Command="{Binding CloseCommand}" Header="退出" />
                </ContextMenu>
            </hc:NotifyIcon.ContextMenu>

            <!--  定义单击托盘图标时的行为  -->
            <hc:Interaction.Triggers>
                <hc:EventTrigger EventName="Click">
                    <!--  单击时同样执行 显示主窗口  -->
                    <hc:EventToCommand Command="hc:ControlCommands.PushMainWindow2Top" />
                </hc:EventTrigger>
            </hc:Interaction.Triggers>
        </hc:NotifyIcon>
    </Grid>
</hc:Window>
